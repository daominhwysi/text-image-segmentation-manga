name: text-image-seg
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas
  - matplotlib
  - pip:
      - roboflow
      - huggingface_hub
      - faker
      - datasets
      - Pillow
